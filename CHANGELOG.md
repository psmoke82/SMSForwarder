# 📋 SMSForwarder 변경 이력 (Changelog)

---

## [v1.2.4] - 2026-08-20

### 🔏 릴리즈 빌드 서명 설정 누락으로 인한 설치 불가 문제 수정
- **문제**: `assembleRelease` 산출물에 signingConfig가 지정되지 않아 서명되지 않은 APK가 생성되어 기기에 설치되지 않던 문제 (직전 릴리즈 이후 반영분)
- **개선**: 기존 사이드로드 설치본 위에 서명 불일치 없이 업그레이드되도록 릴리즈 빌드에 동일 signingConfig를 명시적으로 연결

### 🏷️ 테스트 빌드 식별 표기 추가
- **추가**: 네비게이션 드로어 하단 버전 표기 뒤에 테스트(디버그) 빌드일 때만 `(*Test ver.)` 문구를 덧붙여 정식 빌드와 구분 (예: `v1.2.3 (Build 17) • 2026-08-20 (*Test ver.)`)
- **버전명·빌드 번호·빌드 날짜는 변경하지 않음**: 기존 표기는 그대로 두고 문구만 뒤에 덧붙임
- **`BuildConfig.DEBUG` 기준 자동 분기**: 수동 플래그가 아니므로 릴리즈 빌드에서는 문구가 자동으로 제외되며, 지우는 것을 잊어 정식 빌드에 노출될 여지가 없음

### 🧹 기타
- `.gitignore`에 `cr/` 및 `.claude/` 디렉터리 추가, 중복 기재된 `local.properties` 항목 정리
- `README.md`의 백업/복구 기능 설명을 이번 변경 내용에 맞게 갱신

### 📅 월간 금액합산 화면의 연간 합계를 저장된 연간 합계 금액으로 표시
- **변경**: 연도별 합계를 월별 항목의 단순합으로 계산하던 방식에서, 필터 편집 화면에서 수정 가능한 실제 연간 합계 금액(yearlyTotal)을 그대로 표시하도록 변경. 전달 문자의 `%ytot%` 치환값과 화면 표시가 항상 동일해짐
- **과거 연도는 종전대로 단순합 유지**: yearlyTotal은 진행 중인 당해 연도만 보관하므로, 지난 연도 그룹은 저장된 대응값이 없어 월별 단순합을 그대로 사용
- **필터 카드 누적 합계 기준 정렬**: 카드 상단의 누적 합계도 월별 단순합이 아닌 연도별 합계의 합으로 산출하여 바로 아래 표시되는 연도별 금액과 일관되도록 수정

### 🧮 합계 금액 가감(조정) 시 연간 합계 미반영으로 인한 월간·연간 불일치 수정
- **문제**: 필터 편집 화면의 가감(+/-) 적용 버튼이 월간 합계(monthlyTotal)에만 델타를 반영하고 연간 합계(yearlyTotal)는 그대로 두어, 조정할 때마다 두 값이 어긋나던 문제. 금액합산 확인 화면은 연간 금액을 월별 항목의 합으로 계산해 표시하므로 저장된 연간 합계가 틀어져도 화면상으로는 드러나지 않아 누적 방치됨
- **개선**: 가감 조정은 결제 한 건의 누락/중복 보정을 의미하고 해당 결제는 연간 합계에도 포함되므로, 동일 델타를 연간 합계에도 함께 반영하도록 수정 (차감 시 0원 하한 동일 적용)
- **UI 문구 정정**: 섹션 라벨을 "합계 금액 가감 (조정) · 월간과 연간에 함께 반영"으로 변경하여 동작과 일치시킴
- **직접 입력 칸은 종전대로 독립 유지**: 월간·연간 합계 직접 수정 칸은 "이 값으로 확정"의 의미이므로 각각 독립적으로 동작

### 📊 로컬 및 Google Drive 백업에 월간 금액합산 내역 포함
- **문제**: 백업 스키마(v4)에 `monthlySummaries` 필드와 복원 경로(`restoreMonthlySummaries`, DAO `INSERT OR REPLACE`)가 모두 구현되어 있었으나, `BackupMigrator`가 내보내기 시 빈 리스트로 덮어쓰고 복원 시에도 빈 리스트를 반환하여 월간 금액합산 이력이 실제로는 백업되지 않던 문제
- **개선**: 확정된 과거 월 스냅샷을 백업에 포함하도록 변경. 진행 중인 당월 구간과 필터 자체의 누적액(monthlyTotal 등)은 종전대로 제외하여, 오래된 백업 복원 시 낡은 누적액이 되살아나지 않는 기존 정책을 유지
- **filterId 재매핑**: 합산 내역은 필터 기본키를 참조하는데 이 값이 기기 간에 보존되지 않으므로, 복원 시 필터 저장으로 실제 발급된 id를 수집해 각 합산 레코드의 filterId를 다시 연결. 구버전 백업(필터 id가 0)은 필터명 기준으로 대체 매칭
- **내보내기 데이터 누락 방지**: 백업 화면에서는 월간 합산 StateFlow를 구독하지 않아 `WhileSubscribed` 특성상 빈 값이 기록되던 문제를 리포지토리에서 직접 조회하도록 변경
- **복원 결과 안내 개선**: "3개의 필터와 12건의 금액합산 내역을 복원했습니다" 형태로 합산 이력 건수를 함께 표시
- **하위 호환**: 스키마 버전은 v4 유지. 구버전 백업 파일은 `monthlySummaries` 키 부재 시 이력 없음으로 처리되며, 손상된 개별 레코드는 건너뛰고 필터 복원은 정상 진행
- **진행 중인 당월 실적도 백업 대상에 포함 (구간 라벨 일치 시에만 복원)**: 스냅샷 확정 롤오버는 새 구간의 첫 결제가 들어와야 일어나므로, 롤오버 전에 기기를 교체하면 당월 실적이 통째로 유실되던 공백을 해소. 백업 시 필터의 당월 누적액(monthlyTotal)을 구간 라벨(yy.MM)과 함께 기록하고, 복원 시 **복원하는 기기의 현재 구간 라벨과 일치할 때에만** 반영. 몇 달 뒤 복원하면 라벨이 달라 자동 폐기되므로 "낡은 누적액을 되살리지 않는다"는 기존 정책은 그대로 유지됨
- **당월 실적은 DB 행이 아닌 monthlyTotal 기준**: 화면 표시 및 롤오버 확정 저장이 모두 monthlyTotal을 기준으로 하고 사용자가 직접 수정도 가능하므로, 백업에도 이 값을 기록. 합산 사용이 꺼져 있거나 누적액이 0원인 필터는 기록하지 않음
- **연간 합산액도 동일 방식으로 백업 및 복원 (스키마 v5)**: 연간 합산액(yearlyTotal)은 월별 스냅샷 테이블이 없어 복원 시 0원부터 다시 시작하던 문제를 해소. 백업에 `yearlySummaries` 항목을 신설하여 연도 라벨(yy)과 함께 기록하고, 복원 기기의 현재 연도와 일치할 때에만 반영. 이로써 전달 문자의 `%ytot%` 치환값도 백업 직후 복원 시 그대로 이어짐
- **월간·연간 값 동시 복원 시 덮어쓰기 방지**: 두 값이 같은 필터 레코드에 기록되므로, 항목별로 즉시 저장하지 않고 필터 단위로 모아 1회만 저장하도록 처리
- **하위 호환**: v4 백업 파일은 `yearlySummaries` 키 부재 시 연간 복원 대상 없음으로 처리되며 나머지 복원은 정상 진행
- **테스트 정비**: 구 정책(합산 이력 무조건 제외)을 검증하던 유닛테스트를 새 정책 기준으로 교체하고, 당월 구간 기록·연간 합산액 기록·합산 미사용 및 0원 제외·고아 레코드 제외·구버전 키 부재 처리·손상 레코드 건너뛰기 검증 테스트를 추가 (총 50건 통과)

### 🔒 자동 백업에서 민감 정보 DB 제외
- **문제**: `backup_rules.xml`과 `data_extraction_rules.xml`이 Android Studio 템플릿 상태(규칙 전부 주석 처리)로 방치되어, 수신 문자 원문·전달 대상 번호·결제 금액이 평문으로 저장된 `sms_forwarder.db`가 안드로이드 자동 백업 및 기기 간 전송에 그대로 포함되던 문제
- **개선**: 클라우드 백업(API 31+), 기기 간 전송(device-transfer), 레거시 전체 백업(API 30 이하) 세 경로 모두에서 `sms_forwarder.db` 및 WAL/SHM/journal 부속 파일을 명시적으로 제외
- **영향 없음**: 앱 자체 백업/복원 기능(로컬 및 Google Drive)이 별도로 존재하므로 사용자 데이터 이전 경로는 그대로 유지됨. 환율 캐시와 실행 모드 설정 등 비민감 항목은 자동 백업 대상으로 유지

## [v1.2.3] - 2026-08-17

### 🐛 금액합산 엔진 - 구분자 없이 붙은 "누적" 라벨로 인한 거래금액 누락 수정
- **원인**: 광주카드/광주BC 포맷처럼 거래금액과 "누적" 사이에 공백·줄바꿈 없이 붙어 나오는 문자(예: `6,500원누적 100,052원`)에서, 실제 거래금액(6,500원)까지 누적 총액 판별 로직에 잘못 걸려 0원으로 인식되던 문제 수정
- **개선**: 누적/잔액/한도 등 라벨은 항상 뒤따르는 숫자를 설명한다는 한국어 SMS 관용 표현에 맞춰, 라벨 판별을 앞쪽 문맥(prefix)만 검사하도록 변경. 누적 총액은 여전히 정확히 제외되면서, 취소 거래금액은 정상적으로 음수 반영됨
- **회귀 테스트 추가**: 구분자 없이 붙은 취소/누적 포맷에 대한 유닛테스트 2건 추가

## [v1.2.2] - 2026-08-14

### ↕️ 필터 수정 시 순서 보존 및 신규 필터 순번 정책 개선
- **필터 수정 시 순서 유지**: 필터 수정 화면에서 내용 수정 및 저장 시 기존 설정된 표시 순서(displayOrder)가 0으로 초기화되던 문제를 수정하여 원래 순서가 완벽히 보존되도록 개선
- **신규 필터 순번 자동 할당**: 새 필터 추가 시 기존 필터들의 맨 마지막 순번(max displayOrder + 1)으로 자동 배치

### 📊 월간 금액합산 확인 화면 고도화
- **사용 여부 ON 필터만 표시**: 필터 목록에서 사용 여부(isActive)가 OFF인 필터는 월간 금액합산 화면에 노출되지 않도록 제외
- **당해기간 실시간 합산액 최우선 반영**: 현재 진행 중인 당해월은 결제 실시간 누적 및 필터 상세에서 유저가 직접 수정한 금액(monthlyTotal)을 즉각 1:1로 일치하여 표시
- **미결제 월 0원 자동 채움 타임라인(방안 B) 적용**: 첫 결제/시작 월부터 현재 월까지 결제가 없던 빈 월(예: 6월 실적 후 7월 미결제 시 7월 0원)을 자동으로 채워 연속적인 월별 추이 제공
- **이전 기간 합산액 스냅샷 확정 롤오버**: 새로운 구간의 결제가 시작되는 시점에 이전 기간의 최종 합산액을 해당 월 라벨(yy.MM)로 DB에 확정 스냅샷 저장
- **월별 및 필터별 합산 내역 삭제 기능 지원**: 각 월별 내역 삭제(휴지통 버튼) 및 필터별 전체 합산 이력 일괄 초기화(전체 삭제 버튼) 지원

### 💬 SMS 수신 시 앱 이름 표기 개선
- **'기본 문자' 표기를 실제 SMS 앱 이름('메시지')으로 통일**: 통신사 SMS 브로드캐스트 수신 시 하드코딩된 '기본 문자' 대신 단말기에 설치된 기본 SMS 앱 이름(삼성/구글 '메시지')을 동적으로 감지하여 로그 및 템플릿 치환 변수(%na%)에 '메시지'로 일관되게 표시되도록 개선

### 🎨 필터 목록 카드 레이아웃 및 월별 합산 배지 가독성 개선
- **월별 합산금액 배지 독립 줄 배치**: 필터 제목이 긴 경우 우측 스위치와 겹쳐 배지가 2줄로 쪼개지던 현상을 해결하기 위해, 제목과 수신번호 사이 전용 행에 배지를 배치하여 1줄로 시원하게 표시되도록 개선 (제목 및 수신번호 영역 가로 공간 최대 확보)

### 🛠️ 필터 편집 화면 숫자 입력 커서 위치 튐 현상 수정
- **VisualTransformation 기반 천 단위 쉼표 포맷팅 도입**: 가감 금액 및 월간/연간 합계 직접 수정 시 쉼표 추가로 인해 커서가 중간으로 튀어 숫자가 섞이던 문제를 NumberCommaTransformation을 적용하여 완벽히 해결 (타이핑 시 커서가 항상 끝에 자연스럽게 유지됨)

## [v1.2.1] - 2026-08-14

### ↕️ 필터 목록 순서 변경(Drag & Drop Reordering) 엔진 고도화
- **LazyListState 기반 실시간 좌표 판별 엔진 도입**: 화면 상의 뷰포트 레이아웃 정보를 직접 참조하여 터치 좌표를 정밀하게 히트 테스트함으로써, 한 번 이동 후 재이동 불가 및 엉뚱한 항목 이동 현상 원천 해결
- **상/하단 자동 스크롤(Auto-scroll) 지원**: 화면에 보이지 않는 먼 위치로 드래그 시 뷰포트가 자동으로 부드럽게 스크롤되어, 화면 밖 위/아래 끝까지 한 번의 제스처로 자유롭게 이동 가능
- **완벽한 연속 드래그 및 실시간 햅틱 피드백**: 손가락을 떼지 않고 여러 칸을 연속해서 이동할 수 있으며, 스왑 시 햅틱 진동 및 떠오르는 Elevation/Scale 시각 효과 적용
- **순서 영속화 및 백업 호환성 보장**: `displayOrder` 컬럼을 DB에 추가하여 앱 재시작 및 백업 복원 시에도 설정한 순서가 완벽하게 유지
- **필터 추가(+) 버튼 상단 TopAppBar 이동**: 화면 우측 하단의 플로팅 버튼(FAB)이 마지막 필터 카드의 스위치/수정/삭제 버튼을 가리던 문제를 해결하기 위해, 추가(+) 버튼을 우측 상단 앱바로 이동하고 목록 하단 여백 최적화

### 💰 금액합산 및 실적 관리 엔진 (Amount Summation Engine) 신설
- **자동 결제 금액 추출 및 누적 합산**: 수신 메시지(문자 및 앱 알림) 본문에서 결제 금액을 자동 추출하여 필터별 월간/연간 누적 합계 관리
- **취소 및 환불 금액 자동 차감**: 마이너스 부호 또는 "취소/환불" 키워드 감지 시 누적 합계에서 해당 금액 자동 차감
- **글로벌 외화 결제 원화 환산 지원**: ISO 4217 화이트리스트 기반 40여 개 외화 통화 인식, 실시간 환율 API 조회 및 오프라인 폴백 환율표를 통한 원화 자동 환산
- **실적 집계 구간 및 자동 롤오버 리셋**: 시작일(특정일) 설정 기준 월구간/연구간 자동 계산 및 월말(28/29/30/31일) 달력 보정 자동 리셋 처리
- **원자적 동시성 및 중복 합산 방지**: 메시지 본문 기준 중복 필터링 및 원자적 DB 업데이트를 적용하여 동시 수신 시 금액 누락 및 이중 합산 원천 차단

### 📊 월간 금액합산 확인 화면 신설
- **월간 합산 이력 드릴다운 조회**: 필터명 ➔ 연도 ➔ 월 3단계 계층 구조로 과거 및 현재 월별 실적 내역 확인 가능
- **진행 중 구간 표시 및 스마트 기본 펼침**: 화면 진입 시 최신 연도 자동 펼침 및 현재 집계가 진행 중인 구간에 `(집계중)` 배지(예: `26.08 (집계중)`) 표시

### 📝 템플릿 치환변수 확장 및 로그 저장 한도 확대
- **실적 치환변수 추가**: `%mtot%`(월간 합계), `%ytot%`(연간 합계), `%mtit%`(월구간명, 예: `26.08`), `%ytit%`(연구간명, 예: `26`) 지원
- **포워딩 실행 기록 저장 한도 대폭 확대**: 최대 저장 로그 수를 기존 200건에서 20,000건으로 확대

### 📋 포워딩 실행 기록(로그) 화면 개선
- **실시간 검색 기능 추가**: 필터명, 앱 이름, 메시지 내용, 수신번호 기준으로 로그 실시간 필터링
- **인식 금액 및 환율 상세 정보 표기**: 로그 상세 화면에 추출된 결제 금액, 외화 원문 및 적용 환율 정보 표기

### 💾 백업 & 복구 호환성 엔진 (Backup Migration Engine) 고도화
- **필터 설정 중심 백업 정책**: 실시간 누적 금액과 무관하게 필터 설정 및 규칙을 안전하게 백업/복원하도록 최적화
- **하위 호환성 자동 마이그레이션**: 구버전 백업 파일 복구 시에도 누락 필드 기본값 보정 및 안전한 스키마 자동 승격

### 🎨 UI / UX 및 입력 편의성 개선
- **필터 편집 화면 숫자 입력 개선**: 시작 특정일 및 가감 금액 입력 필드 높이 잘림 수정 및 자유로운 숫자 편집/삭제 지원
- **필터 목록 카드 시각화**: 필터 카드 타이포그래피 슬림화 및 월간 합계 배지(예: `26.08월 230,894원`) 표시

---

## [v1.0.11] - 2026-08-07

### 📐 UI / UX 슬림화 및 콤팩트 디자인 개편
- **필터 목록 카드 레이아웃 정돈**: 카드 상하 내부 여백 축소 (`16.dp` ➡️ `10.dp`) 및 필터 이름 ↔ 수신처 간격 축소 (`10.dp` ➡️ `2.dp`)
- **포워딩 실행 기록 카드 높이 60% 슬림화**: 기존 `SuggestionChip`을 콤팩트 **미니 배지(`Surface`)**로 교체하고 접기/펼치기 아이콘을 동일 라인에 통합
- **실시간 발송 텍스트 미리보기 샘플 개선**: 여러 앱 선택 시 콤마 목록 대신 첫 번째 대표 앱 이름 **1개만 샘플로 가공**되어 표시되도록 개선

### 🏷️ 빌드 & 바이너리 자동화 (Build & Release Automation)
- Gradle 빌드 시 출력 APK 파일명이 `SMSForwarder-v1.0.11-rel.apk`로 자동 지정
- GitHub Release 생성 시 `SMSForwarder-v1.0.11-rel.apk` 바이너리 자동 업로드

---

## [v1.0.8] - 2026-08-07

### 🏷️ 빌드 & 바이너리 자동화
- 버전 포함 APK 파일명 산출 설정 적용 (`androidComponents`)
- GitHub Release 자동 바이너리 게시 규칙 등록
